{
  "name": "tambola",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rimraf docs",
    "build": "npm run clean && parcel build index.html --no-source-maps --dist-dir docs && copyfiles CNAME docs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "parcel": "^2.8.3",
    "rimraf": "^4.3.0"
  }
}
