{
  "name": "tambola",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "install":"npm install --save-dev copy-files-from-to",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf  docs",
    "build": "npm run clean && parcel build index.html --dist-dir docs && copy-files-from-to CNAME docs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.3",
    "rimraf": "^4.3.0"
  }
}
